local a={}local b={}b.__index=b;local c=2000;function a.new(d)d=d or{}return setmetatable({pollIntervalMs=d.pollIntervalMs or c,shown=false,lastPoll=nil},b)end;function b:show()self.shown=true;self.lastPoll=nil end;function b:hide()self.shown=false end;function b:isShown()return self.shown end;function b:poll(e,f)if not self.shown then return false end;if self.lastPoll~=nil and f-self.lastPoll<self.pollIntervalMs then return false end;e:queryAll(f)self.lastPoll=f;return true end;return a