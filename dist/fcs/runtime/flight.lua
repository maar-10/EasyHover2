local a=require("fcs.comauto")local b={}b.__index=b;local function c(d)local e=d and d.byId and d.default and d.byId[d.default]local f=e and e.feel and e.feel.trimDir;return f or-1 end;function b.new(g)return setmetatable({loop=g.loop,pilot=g.pilot,registry=g.registry,moveEps=g.moveEps or 0.5,fuel=g.fuel,minFuel=g.minFuel or 0.05,engaged=false,gndSafety=true,positionHold=false,fuelPump=false,flightMode=g.registry and g.registry.default or"PRECISION",parked=false,trimDir=c(g.registry),_needReset=false,_loopHz=0,noFuel=false},b)end;function b:handleCommand(h)local i=h and h.k;if i=="gndSafety"then self.gndSafety=h.on and true or false;return true elseif i=="engage"then if self.gndSafety or self.noFuel then return false end;local j=self.fuel and self.fuel()or nil;if j~=nil and j<self.minFuel then return false end;self.engaged=true;self._needReset=true;return true elseif i=="disengage"then self.engaged=false;self.positionHold=false;self.pilot:setPositionHold(false)self.loop:arm(false)return true elseif i=="positionHold"then self.positionHold=h.on and true or false;self.pilot:setPositionHold(self.positionHold)return true elseif i=="fuelPump"then self.fuelPump=h.on and true or false;return true elseif i=="clearDamped"then self.loop:clearDamped()return true elseif i=="flightMode"then local d=self.registry;local e=d and d.byId[h.id]if not e then return true end;self.loop:setActive(e)self.pilot:setMode(e.policy,e.feel)self.flightMode=h.id;self.trimDir=e.feel and e.feel.trimDir or self.trimDir;return true elseif i=="flightTrim"then local k=h.dir and h.dir<0 and-1 or 1;self.trimDir=k;if self.pilot.setTrimDir then self.pilot:setTrimDir(k)end;return true elseif i=="comAuto"then if h.op=="abort"then if self.comAuto then self.comAuto:abort("ABORT")end;return true end;if h.op=="start"then if not self.engaged or self.gndSafety then return false end;self.comAuto=a.new({spanFwd=h.spanFwd or h.span or 1,spanRight=h.spanRight or h.span or 1})self.comAuto:start(self._lastMeas or{})return true end;return false end;return false end;function b:_parked(l,m)if self.comAuto and self.comAuto:active()then return false end;if not(m and m.onGround==true)then return false end;if l and l.up==true then return false end;local n=self.moveEps;return math.abs(m.vSpeed or 0)<n and math.abs(m.swayVel or 0)<n and math.abs(m.surgeVel or 0)<n end;function b:_checkFuel(m)if not self.fuel then return end;local j=self.fuel()if j==nil then return end;if j<self.minFuel then if not self.noFuel then self.noFuel=true;self.engaged=false;self.positionHold=false;if self.pilot.setPositionHold then self.pilot:setPositionHold(false)end;self.pilot:reset(m)self.loop:arm(false)end elseif self.noFuel and j>=self.minFuel*2 then self.noFuel=false end end;function b:step(o,l,m)self._lastMeas=m;local p=self.comAuto and self.comAuto:active()if p then l={}end;self:_checkFuel(m)if self.engaged then if self._needReset then self.pilot:reset(m)self._needReset=false end;self.parked=self:_parked(l,m)if self.parked then self.pilot:reset(m)self.loop:arm(false)else if p then local q=self.lastDiag and self.lastDiag.duties;local r=self.comAuto:tick(o,m,q,self.loop:getMode())local s=self.loop.scheme;if s and s.pitchPid then if r.captureKi and r.captureKi>0 then if not self._comKiSaved then self._comKiSaved={p=s.pitchPid.ki,r=s.rollPid.ki}end;s.pitchPid.ki=r.captureKi;s.rollPid.ki=r.captureKi elseif self._comKiSaved then s.pitchPid.ki=self._comKiSaved.p;s.rollPid.ki=self._comKiSaved.r;self._comKiSaved=nil end end;if r.captured and self.loop.mixer and self.loop.mixer.setCom then self.loop.mixer:setCom({fwd=r.captured.fwd,right=r.captured.right,spanFwd=self.comAuto.spanFwd,spanRight=self.comAuto.spanRight})end;if r.setpoints then self.loop:setpoints(r.setpoints)end;self.loop:arm(true)if r.done then self.engaged=false;self.loop:arm(false)end else self.loop:setpoints(self.pilot:update(o,l or{},m))self.loop:arm(true)end end else self.parked=false;self.loop:arm(false)end;local t=self.loop:cycle(o,m)self.lastDiag=t;if o>0 then self._loopHz=1/o end;return self:snapshot(t,m)end;function b:snapshot(t,m)local u=m or{}return{engaged=self.engaged,gndSafety=self.gndSafety,positionHold=self.positionHold,fuelPump=self.fuelPump,parked=self.parked,noFuel=self.noFuel,mode=self.parked and"PARKED"or(t and t.mode or self.loop:getMode()),flightMode=self.flightMode,trimDir=self.trimDir,altitude=u.baroMsl or u.altitude,vSpeed=u.vSpeed,heading=u.heading,yawRate=u.yawRate,swayPos=u.swayPos,surgePos=u.surgePos,onGround=u.onGround,loopHz=self._loopHz,comAuto=self.comAuto and{phase=self.comAuto.phase,abortReason=self.comAuto.abortReason,captured=self.comAuto.captured}or nil}end;return b